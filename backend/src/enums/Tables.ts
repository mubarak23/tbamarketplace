enum Tables {
  TEMPORARY_USERS = 'temporary_users',
  USERS = 'users',
  TICKETS = 'tickets',

  SMS_SEND_LOGS = 'sms_send_logs',
  CRON_RUNS = 'cron_runs',
  PLACE_BIDS = 'place_bids',
  SELECTED_BID = 'selected_bid',
  SELLER_NFT = 'seller_nft',
  TBA_BIDS = 'tba_bids',

  WALLETS = 'wallets',
}

export default Tables;
